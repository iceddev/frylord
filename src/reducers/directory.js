'use strict';

function directory(state = {}, { type, payload }){
  switch(type){
    case '':
      // handle payload
      return state;
    default:
      return state;
  }
}

module.exports = directory;
